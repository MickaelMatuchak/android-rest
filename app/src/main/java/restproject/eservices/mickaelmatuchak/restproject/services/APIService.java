package restproject.eservices.mickaelmatuchak.restproject.services;

import java.util.List;

import restproject.eservices.mickaelmatuchak.restproject.models.Post;

import retrofit2.http.GET;
import retrofit2.http.POST;
import retrofit2.http.Field;
import retrofit2.http.FormUrlEncoded;

import retrofit2.Call;

public interface APIService {
    /*
    @GET("/posts")
    Call<List<Post>> getPosts();
    */

    /*
    @POST("/posts")
    @FormUrlEncoded
    Call<Post> addPost(@Field("name") String name, @Field("message") String message);
    */
}
